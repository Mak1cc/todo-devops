apiVersion: v1
kind: Service
metadata:
  name: todo-devops-service
spec:
  type: NodePort
  selector:
    app: todo-devops
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080